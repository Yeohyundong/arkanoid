# Arkanoid — 게임 설계 문서

> 현재 구현 기준. v2 및 Unity 포팅은 Out of Scope 섹션 참고.

---

## 1. 개요

| 항목 | 내용 |
|------|------|
| 장르 | 클래식 벽돌깨기 + Heat 통합 점수 시스템 + 2개 모드 (Arcade / Wave) |
| 플랫폼 | Web (TypeScript + HTML5 Canvas + Vite) |
| 크로스플랫폼 | 데스크톱(PC) + 모바일(터치) |
| 배포 | Cloudflare Pages (정적 호스팅) |
| 향후 계획 | v2 고도화 → Unity 포팅 |

## 2. 설계 원칙

1. **천장을 뚫는 재미, 실패로 벌주지 않음** — 도전은 "만점에 얼마나 가까운가"로 표현. Arcade는 전통성을 위해 목숨(3)을 유지하지만, Wave는 목숨 없이 공간적 압박(하강 임계선)으로 승부를 본다.
2. **운은 다양성, 결과는 실력** — 아이템 블록 등장 위치는 랜덤이지만 플레이어 눈에 보이고, 공을 조준해 실력으로 먹을 수 있어야 한다.
3. **템포 유지** — 공의 흐름을 끊는 메커니즘(Sticky, Bomb-next 등)은 보류. 즉각 발동/지속 버프를 선호.
4. **스트레스 최소화** — 아이템은 떨어져도 바닥에서 튕겨 올라오고, 동일 버프는 시간/크기 누적(리셋 아님). 스테이지 전환 시엔 모든 버프 깨끗하게 초기화.

## 3. 컨셉

- **비주얼 스타일**: 기하학 / 네온
- **분위기**: 재미 위주, 도파민 파티
- **시그니처 메커니즘**: 공 속도 가속 + Heat Tier 점수 배수 + Fireball 불꽃 trail

## 4. 코어 메커니즘

### 패들
- 위치 기반 반사각: 패들 중앙 → 수직 반사, 가장자리일수록 큰 반사각 (최대 60°)
- 좌우 경계 안에서만 이동 (현재 너비 기준 clamp)
- 기본 너비 **110px**

### 공
- 패들/벽/벽돌 어디든 튕길 때마다 속도 +α, stack +1
- **최대 속도 캡**: 1000 px/s
- **최소 수직 성분 clamp**: `|dirY| ≥ 0.3` — 가로로 눕는 루프 방지 (특히 멀티볼 분기 시)
- **리셋**: 공이 바닥 통과 시 해당 공만 제거. 모든 공 소실 시 목숨 차감(Arcade) 또는 그냥 리스폰(Wave).
- 각 공은 **독립적인 stack/heat tier** 보유 — 한 공 잃어도 다른 공에 영향 없음

### Heat 시스템 (콤보 통합)
공이 튕길 때마다 `stack` 1 증가. stack이 임계값 이상이면 **Heat Tier** 상승:

| Tier | 조건 | 점수 배수 | 색상 |
|------|------|----------|------|
| COOL  | stack ≥ 0  | ×1 | 청록 |
| WARM  | stack ≥ 5  | ×2 | 주황 |
| HOT   | stack ≥ 15 | ×3 | 빨강 |
| BLAZE | stack ≥ 30 | ×5 | 노랑 |

- 점수 공식: `10 × heat_mult`
- 시각 피드백: 공 주변 오라(얇게 유지), 5 stack마다 공 위치에 플로팅 숫자 (5/15/30은 큰 글자 + shake)
- Stack 리셋: 바닥 아웃 시 해당 공만. 패들 터치로는 리셋 안 됨.

### 공 발사
- 라운드 시작 / 모든 공 소실 직후: 공이 패들 위 대기
- 화면 탭 (canvas 외부 포함) → 우상단 30° 발사

### 벽돌
- HP 1~9. 맞을 때마다 -1, 0이면 파괴. 숫자 표시.
- **아이템 블록**은 별도 (5.1 참조)

### 점수
- 벽돌 1개 파괴: **10점 × heat 배수**
- 모드별 최고 점수 개별 저장

## 5. 아이템 시스템 (Arcade/Wave 공통)

### 5.1 아이템 블록 (드롭 소스)
- **벽 맨 아래 줄 바로 아래**에 독립 HP1 floater 벽돌로 스폰 (일반 벽돌과 섞이지 않음)
- 본체는 어두운 중성색 `#1e1e2e`, 외곽이 아이템 컬러로 pulse, 가운데 글자는 항상 선명 (글로우 포함)
- **4초 간격**으로 1개씩 스폰 (공이 붙어있는 동안 타이머 정지)
- **10초 수명**: 처음 6초 느린 pulse, 마지막 4초 빠른 경고 블링크
- 만료 시 벽돌 자체가 제거 (벽에 자국 안 남김)
- 임계선 근접 상황에선 스폰 스킵 (안전장치)

### 5.2 아이템 캡슐 (획득)
- 아이템 블록 파괴 시 해당 위치에서 캡슐 낙하 (속도 220 px/s)
- 패들에 닿으면 효과 발동, 소멸
- **바닥/상단 벽에 튕김** → 잡힐 때까지 오르락내리락 (스트레스 최소화 원칙)

### 5.3 아이템 종류

| 글자 | 색 | 효과 | 지속 |
|-----|-----|------|------|
| **M** Multi-ball | 초록 | 공 2개 추가 (현재 공의 위치/속도/stack/heat 상속, ±30° 분기) | 영구 |
| **E** Enlarge    | 노랑 | 패들 **+40px 스택** (좌우 +20px씩), 최대 **10 스택** (= 최대 너비 510px) | 스택당 8초, 만료 3초 전부터 사라질 부분 점멸 |
| **F** Fireball   | 주홍 | 모든 공이 벽돌 관통 (튕기지 않고 부숨). **공 뒤로 불꽃 trail** (노랑→주황→빨강 페이드) | 5초 |
| **H** Heat boost | 빨강 | 모든 공의 stack +15 즉시 (속도 비례 증가) | 영구 (stack) |

- 동일 버프 **중복 획득 시 시간/크기 누적** (E는 스택 누적, 나머지는 타이머 누적)
- E가 최대 스택일 때 추가로 먹으면 가장 먼저 만료될 스택을 연장
- **스테이지 클리어 시 패들 효과(E) 완전 리셋**. 공 효과(F/H/멀티볼)는 공이 새로 생성되면서 자동 리셋.

## 6. 게임 모드

### 6.1 ARCADE (고전 스테이지 진행)
- **10개 고정 스테이지**, 순차 진행
- **목숨 3개** (HUD에 공 아이콘으로 표시). 모든 공 소실 시 -1. 목숨 0이면 게임오버.
- 스테이지 클리어 시 체크포인트 저장 (다음 스테이지 / 점수 / 남은 목숨)
- 최종 스테이지 클리어 시 "YOU WIN" + 최고 점수 제출
- Continue로 재개 가능

**스테이지 테마 구성**:
| # | 이름 | 특징 |
|---|------|------|
| 1 | Welcome Wall | HP1 직사각형 (입문) |
| 2 | Stripes | HP1/HP2 교차 밴드 |
| 3 | Frame | 외곽 HP2, 내부 HP1 |
| 4 | H Letter | 알파벳 H |
| 5 | Diamond | 다이아몬드, 중앙 HP2 |
| 6 | Checkerboard | 체커 패턴 |
| 7 | Pyramid | 삼각형, 외곽 HP3 |
| 8 | Zigzag | 대각선 계단 |
| 9 | Fortress | 중첩 요새, 코어 HP1 |
| 10 | Boss Wall | 5층 HP(5/4/3/2/1) 대칭 |

기본 속도: 500 → 620 px/s 완만 상승 (스테이지별).

### 6.2 WAVE (하강 엔드리스)
- **단일 레벨**, 초기 3줄로 시작
- **매 N초**마다 모든 벽돌 한 칸 하강 + 맨 위에 새 열 1개 추가. N은 웨이브별 15초 → 최소 5초 (-1초씩).
- 새 열은 **패턴 풀**에서 가중 랜덤 선택:
  - Dense 풀(7~9 벽돌, 명확한 대칭): 저 웨이브에 집중
  - Mixed 풀(5~7 벽돌, 주기적 구멍): 웨이브 진행하며 비중 상승
  - HP 분포도 웨이브에 따라 가중 (1~3 웨이브 HP1만 → 11+ 웨이브 HP1:2:3 = 35/40/25%)
- **임계선**(패들 위 3행)을 벽돌 하나라도 넘으면 **GAME OVER**
- 목숨 없음 — 공 떨어지면 리스폰, 실패는 공간 관리 실패로만 발생

## 7. 조작

**통합 입력 — Pointer Events (마우스 = 터치)**

| 액션 | 입력 |
|------|------|
| 패들 이동 | 포인터 x 위치 (canvas 내부) |
| 공 발사 / 다음 진행 | 화면 클릭 / 탭 (canvas 외부 클릭도 발사) |
| 메뉴 선택 | 버튼 탭 |

키보드 미지원 (v2 여지).

## 8. 화면 레이아웃

- **게임판 내부 좌표계**: 540 × 960 (9:16 세로)
- **모바일**: 화면 전체를 게임판으로 채움
- **데스크톱**: 게임판 중앙 배치, 좌우 여백 장식

### HUD (상단)
```
Arcade:  SCORE xxxx      STAGE 2/10      BEST yyyy
         ● ● ●    (남은 목숨 = 공 아이콘, SCORE 아래)

Wave:    SCORE xxxx       WAVE 5         BEST yyyy
         (임계선 점선 빨강 표시)
```

- `BEST`가 현재 점수로 돌파되는 순간 → 초록색으로 전환 + 실시간 갱신
- 게임 오버 시 "NEW BEST!" 표시 (실제 갱신된 경우만)
- 공 위 플로팅 콤보 숫자는 모든 모드 공통

## 9. 메인 메뉴 흐름

```
          ARKANOID
   [ ARCADE       BEST x ]   ← 노랑 (고전 진행)
   [ CONTINUE  arcade cp  ]   ← 파랑 (체크포인트 있을 때만)
                              (구역 분리)
   [ WAVE         BEST y ]   ← 초록 (엔드리스)
```

- Arcade와 Continue는 시각적으로 붙여 그룹화. Wave는 아래 따로 배치.
- Continue는 arcade 체크포인트가 있을 때만 활성.

## 10. 저장 시스템

- **MVP**: 브라우저 localStorage (기기별)
- **`SaveManager` 추상화 레이어** — 내부만 교체해서 클라우드/IndexedDB 확장 가능
- **저장 항목** (key: `arkanoid:save:v4`):
  - `arcadeHighScore`
  - `waveHighScore`
  - `arcadeCheckpoint` (stageIndex / score / lives) — Arcade 전용
- **타이밍**:
  - 아케이드 스테이지 클리어 → 체크포인트 저장
  - 아케이드 게임오버(목숨 0) 또는 최종 클리어 → 체크포인트 삭제 + 해당 모드 최고 점수 갱신
  - 웨이브 게임오버 → 웨이브 최고 점수 갱신

## 11. 클래스 구조

```
Game                     // 전체 컨트롤러 + 상태 머신
 ├─ mode: 'arcade' | 'wave'   // 모드 분기
 ├─ state: menu/playing/cleared/ending
 ├─ Ball[]               // 멀티볼, 각 공이 stack/heat/fireball/trail 독립
 ├─ Paddle               // 너비 동적, enlarge 스택 배열
 ├─ Brick[]              // HP(mutable y for wave descent), itemType, itemSpawnedAt
 ├─ Item[]               // 낙하 중인 캡슐 (바운스)
 ├─ ComboFloater[]       // 공 위 플로팅 숫자
 ├─ InputHandler         // Pointer Events + window-level tap
 ├─ SaveManager          // localStorage 추상화 (모드별 점수 + 체크포인트)
 └─ MainMenu             // 3버튼 (ARCADE/CONTINUE/WAVE)
```

## 12. Out of Scope — v2 이후

### v2 (두 번째 웹 버전) 후보
- 추가 아이템: Laser(L), Sticky(S), Bomb(X) 등 — **템포/스트레스 검토 필요**
- 특수 벽돌: 폭발, 불사, 이동
- 아웃게임 상점 (코인 수집 → 영구 업그레이드)
- Wave 모드 심화: 보스 웨이브, 특수 이벤트
- 사운드 / BGM
- 파티클 / 화면 흔들림
- 타임어택 / 로그라이트 빌드
- 레벨 에디터
- 키보드 조작 지원

### Unity 포팅 단계
- TS 클래스를 C# MonoBehaviour로 이식
- 포스트프로세싱 (Bloom, Glow)
- 클라우드 저장 — `SaveManager` 교체
- 플랫폼별 빌드 (WebGL, Windows, Android, iOS)
