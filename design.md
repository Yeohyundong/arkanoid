# Arkanoid — 게임 설계 문서

> 현재 구현 기준. v2 및 Unity 포팅은 Out of Scope 섹션 참고.

---

## 1. 개요

| 항목 | 내용 |
|------|------|
| 장르 | 클래식 벽돌깨기 + Heat/콤보 통합 점수 시스템 |
| 플랫폼 | Web (TypeScript + HTML5 Canvas + Vite) |
| 크로스플랫폼 | 데스크톱(PC) + 모바일(터치) |
| 배포 | Cloudflare Pages (정적 호스팅) |
| 향후 계획 | v2 고도화 → Unity 포팅 |

## 2. 설계 원칙

1. **천장을 뚫는 재미, 실패로 벌주지 않음** — 공 잃어도 게임 오버 없음. 스코어 어택 장르. 도전은 "만점에 얼마나 가까운가"로 표현한다.
2. **운은 다양성, 결과는 실력** — 아이템 블록 등장 위치는 랜덤이지만 플레이어 눈에 보이고, 공을 조준해 실력으로 먹을 수 있어야 한다.
3. **템포 유지** — 공의 흐름을 끊는 메커니즘(Sticky, Bomb-next 등)은 보류. 즉각 발동/지속 버프를 선호.
4. **스트레스 최소화** — 아이템은 떨어져도 바닥에서 튕겨 올라오고, 목숨 패널티는 없으며, 동일 버프는 시간 누적(리셋 아님).

## 3. 컨셉

- **비주얼 스타일**: 기하학 / 네온
- **분위기**: 재미 위주, 도파민 파티
- **시그니처 메커니즘**: 공 속도 가속 + Heat Tier 점수 배수

## 4. 코어 메커니즘

### 패들
- 위치 기반 반사각: 패들 중앙 → 수직 반사, 가장자리일수록 큰 반사각 (최대 60°)
- 좌우 경계 안에서만 이동
- `E` 아이템 시 너비 1.5배, 8초 유지 (중복 획득 시 **시간 누적**)

### 공
- 패들/벽/벽돌 어디든 튕길 때마다 속도 +α, stack +1
- **최대 속도 캡**: 플레이 불가 수준 방지
- **최소 수직 성분 clamp**: `|dirY| ≥ 0.3` — 가로로 눕는 루프 방지 (특히 멀티볼 분기 시)
- **리셋**: 공이 바닥 통과 시 해당 공만 제거. 모든 공 소실 시 패들 위 새 공 1개 생성 (속도/stack 초기화)
- 각 공은 **독립적인 stack/heat tier** 보유 — 한 공 잃어도 다른 공에 영향 없음

### Heat / 콤보 시스템 (통합)
공이 튕길 때마다 `stack` 1 증가. stack이 일정 값 이상이면 **Heat Tier** 상승:

| Tier | 조건 | 점수 배수 | 색상 |
|------|------|----------|------|
| COOL  | stack ≥ 0  | ×1 | 청록 |
| WARM  | stack ≥ 5  | ×2 | 주황 |
| HOT   | stack ≥ 15 | ×3 | 빨강 |
| BLAZE | stack ≥ 30 | ×5 | 노랑 |

- 점수 공식: `벽돌 기본점수 × heat_mult` (별도 콤보 배수 없음)
- 시각 피드백: 공 주변 오라 색이 tier 색으로 변화, 5 stack마다 공 위치에 숫자 플로팅 (5/15/30은 큰 글자 + shake)
- stack 리셋 조건: **바닥 아웃만**. 패들 터치는 stack 유지.

### 공 발사 (라운드 시작)
- 라운드 시작 / 모든 공 소실 직후: 공이 패들 위 대기
- 화면 탭(클릭, **canvas 밖 포함**) → 우상단 30° 발사

### 벽돌
- **HP 1~9** 지원. 맞을 때마다 HP-1, 0이면 파괴. 표면에 HP 숫자 표시 (아이템 블록은 아이템 글자 표시).
- 레벨 포맷: 텍스트 배열의 각 문자가 HP (`1`~`9`, `.`은 빈 칸).
- **아이템 블록** — 런타임에 지정됨 (아래 5.1 참조).

### 점수
- 벽돌 1개 파괴: **10점 × heat 배수**
- 최고 점수 / 스테이지 진행 저장

## 5. 아이템 시스템

### 5.1 아이템 블록 (드롭 소스)
- **스테이지 시작 시 0개**. 타이머로 주기 생성.
- **4초에 1번**, 현재 맨 아래 줄(벽돌이 남아있는 최하단 row)의 빈 블록 중 랜덤 1개를 아이템 블록으로 지정
- 수명 **10초** — 파괴되지 않으면 해제되어 일반 벽돌로 복귀
- 표시:
  - 0~6초: 느린 pulse (초록 외곽 + 가운데 글자)
  - 6~10초: **빠른 경고 블링크** + 굵은 외곽
- 공이 패들에 붙어있는 동안은 스폰 타이머 정지 (첫 발사 전 쌓이는 것 방지)

### 5.2 아이템 캡슐 (획득)
- 아이템 블록 파괴 시 해당 위치에서 캡슐 낙하 (속도 220px/s)
- 패들에 닿으면 효과 발동, 소멸
- **바닥 통과 시 튕겨 올라감** (상단 벽에서 다시 튕김) — 잡힐 때까지 오르락내리락

### 5.3 아이템 종류

| 글자 | 색 | 효과 | 지속 |
|-----|-----|------|------|
| **M** Multi-ball | 초록 | 공 2개 추가 (현재 공의 위치/속도/stack/heat 상속, ±30° 분기) | 영구 |
| **E** Enlarge    | 노랑 | 패들 너비 ×1.5 | 8초 |
| **F** Fireball   | 주홍 | 모든 공이 벽돌 관통 (튕기지 않고 부숨) | 5초 |
| **H** Heat boost | 빨강 | 모든 공의 stack +15 즉시 (속도 비례 증가) | 영구 (stack) |

- 동일 버프 **중복 획득 시 시간 누적** (리셋 아님 — 스트레스 최소화)

## 6. 조작

**통합 입력 — Pointer Events (마우스 = 터치)**

| 액션 | 입력 |
|------|------|
| 패들 이동 | 포인터 x 위치 (canvas 내부) |
| 공 발사 / 다음 진행 | 화면 클릭 / 탭 (canvas 외부 클릭도 발사) |
| 메뉴 선택 | 버튼 탭 |

키보드 미지원 (v2 여지).

## 7. 화면 레이아웃

- **게임판 내부 좌표계**: 540 × 960 (9:16 세로)
- **모바일**: 화면 전체를 게임판으로 채움
- **데스크톱**: 게임판 중앙 배치, 좌우 여백 장식

### HUD
- SCORE (좌상단)
- STAGE (상단 중앙)
- 공 위 플로팅 콤보 숫자 (좌상단 HUD 숫자는 제거 — 정적 HUD 최소화)

## 8. 게임 흐름

```
메인 메뉴
 ├─ 새 게임
 └─ 이어하기 (저장 있을 때만 활성)

새 게임 → Stage 1 → Stage 2 → Stage 3 → 엔딩

스테이지 클리어: 축하 연출 → 탭 → 다음 스테이지
최종 엔딩: 총점 표시 → 탭 → 메인 메뉴
```

실패(모든 공 소실) 시에도 게임 오버 없음 → 패들 위 새 공 생성하여 계속 플레이.

## 9. 스테이지 구성

### MVP: 3개 스테이지 직접 디자인

레벨 포맷 — 텍스트 배열. 문자는 HP(1~9), `.`은 빈 칸.

```typescript
{
  rows: [
    '..........',
    '.11111111.',
    '1111111111',
    '1111111111',
    '.11111111.',
  ],
  baseSpeed: 500,
}
```

### 난이도 곡선
- **Stage 1** — 튜토리얼: 벽돌 적음, HP 1만
- **Stage 2** — 표준: HP 1~2 혼합
- **Stage 3** — 도전: HP 1~3, 레이아웃 복잡

## 10. 저장 시스템

- **MVP**: 브라우저 localStorage (기기별)
- **`SaveManager` 추상화 레이어** — 내부만 교체해서 클라우드/IndexedDB 확장 가능
- **저장 항목**:
  - **체크포인트**: 다음 스테이지 인덱스, 누적 점수
  - **최고 점수**
- **타이밍**:
  - 스테이지 클리어 → 체크포인트 저장
  - 엔딩 → 체크포인트 삭제 + 최고 점수 갱신
- **Continue**: 체크포인트가 있을 때만 활성

## 11. 클래스 구조

```
Game                     // 전체 컨트롤러 + 상태 머신
 ├─ Ball[]               // 멀티볼 지원, 각 공이 stack/heat/fireball 독립 보유
 ├─ Paddle               // 너비 동적(enlarge)
 ├─ Brick[]              // HP, itemType, itemSpawnedAt
 ├─ Item[]               // 낙하 중인 캡슐
 ├─ ComboFloater[]       // 공 위 플로팅 숫자
 ├─ InputHandler         // Pointer Events (window-level tap 포함)
 ├─ SaveManager          // localStorage 추상화
 └─ MainMenu             // 메뉴 렌더/히트 테스트
```

## 12. Out of Scope — v2 이후

### v2 (두 번째 웹 버전) 후보
- 추가 아이템: Laser(L), Sticky(S), Bomb(X) 등 — **템포/스트레스 검토 필요**
- 특수 벽돌: 폭발, 불사, 이동
- 아웃게임 상점 (코인 수집 → 영구 업그레이드)
- 사운드 / BGM
- 파티클 / 화면 흔들림
- 타임어택 / 엔들리스 모드
- 레벨 에디터
- 키보드 조작 지원

### Unity 포팅 단계
- TS 클래스를 C# MonoBehaviour로 이식
- 포스트프로세싱 (Bloom, Glow)
- 클라우드 저장 — `SaveManager` 교체
- 플랫폼별 빌드 (WebGL, Windows, Android, iOS)
