# Arkanoid — 게임 설계 문서

> MVP 기준. v2 및 Unity 포팅은 Out of Scope 섹션 참고.

---

## 1. 개요

| 항목 | 내용 |
|------|------|
| 장르 | 클래식 벽돌깨기 + 속도 메커니즘 트위스트 |
| 플랫폼 | Web (TypeScript + HTML5 Canvas + Vite) |
| 크로스플랫폼 | 데스크톱(PC) + 모바일(터치) |
| 배포 | Cloudflare Pages (정적 호스팅) |
| 향후 계획 | v2 고도화 → Unity 포팅 |

## 2. 컨셉

- **비주얼 스타일**: 기하학 / 네온
- **분위기**: 재미 위주, 도파민 파티
- **시그니처 메커니즘**: 공 속도 가속 + 패들 접촉 시 리셋

## 3. 코어 메커니즘

### 패들
- 위치 기반 반사각: 패들 중앙에 맞으면 수직 반사, 가장자리일수록 큰 반사각
- 좌우 경계 안에서만 이동

### 공
- **속도 가속**: 튕길 때마다 속도 +α 누적 (벽/벽돌/패들 모두 포함)
- **리셋 조건**:
  - 공을 잃었을 때(바닥 통과) → 기본 속도로 리셋
  - 스테이지 시작 시 → 기본 속도로 리셋
  - (패들 접촉 시엔 리셋 ❌ — 속도 유지)
- **상한선**: 최대 속도 캡 (플레이 불가 수준 방지)
- **FEVER 보상**: 공이 빠를수록 보상 증가
  - 벽돌 점수 배수 상승 (속도 구간별)
  - 시각 피드백: 공 색상이 차가운 색 → 뜨거운 색으로 변화
  - (v2) 아이템 드롭율 상승
- 벽/벽돌 반사는 입사각 = 반사각

### 공 발사 (라운드 시작)
- 라운드 시작 / 공 잃은 직후: 공이 패들 위에 붙어서 대기
- 화면 탭(클릭) → 우상단 30도 고정 각도로 발사 (클래식 알카노이드 스타일)

### 생명 / 실패 조건
- 기본 생명 3개
- 공이 바닥에 떨어지면 생명 1 감소
- 생명 0이 되면 게임 오버

### 벽돌
- MVP: 일반 벽돌 1종 (한 번 맞으면 파괴)
- 설계는 **`type` 속성 확장 가능한 구조**로 (v2에서 튼튼/폭발/아이템 추가)

### 점수
- 벽돌 1개 파괴: **10점**
- 스테이지 클리어 보너스: **남은 시간에 비례한 가산점**
- 최고 점수 / 스테이지 진행은 저장됨

## 4. 조작

**통합 입력 — Pointer Events 사용 (마우스 드래그 = 터치 드래그)**

| 액션 | 입력 |
|------|------|
| 패들 이동 | 포인터(마우스/터치) x 위치 추적 |
| 공 발사 / 다음 진행 | 화면 클릭 / 탭 |
| 일시정지 | 화면 우측 상단 UI 버튼 |

키보드 미지원 (v2 여지 남김).

## 5. 화면 레이아웃

- **게임판 내부 좌표계**: 540 × 960 (9:16 세로)
- **모바일**: 화면 전체를 게임판으로 채움
- **데스크톱**: 게임판 중앙 배치, 좌우 여백은 HUD + 네온 장식 (아케이드 캐비닛 느낌)

### HUD 요소
- 현재 점수
- 생명 개수
- 현재 스테이지
- 일시정지 버튼

## 6. 게임 흐름

```
메인 메뉴
 ├─ 새 게임
 ├─ 이어하기 (저장 있을 때만 활성)
 ├─ 설정
 └─ 크레딧

새 게임 → Stage 1 → Stage 2 → Stage 3 → 엔딩

각 스테이지 클리어:
  축하 연출 → 화면 탭 → 다음 스테이지

게임 오버:
  화면 탭 → 메인 메뉴

최종 엔딩:
  총점 표시 + 축하 연출 → 화면 탭 → 메인 메뉴
```

## 7. 스테이지 구성

### MVP: 3개 스테이지 직접 디자인

**레벨 포맷 — 텍스트 배열**

```typescript
const STAGE_1 = [
  "..........",
  ".BBBBBBBB.",
  "BBBBBBBBBB",
  "BBBBBBBBBB",
  ".BBBBBBBB.",
  "..........",
];
// B = 일반 벽돌, . = 빈 공간
// (v2에서 T, E, I 등 추가 예정)
```

### 난이도 곡선
- **Stage 1** — 튜토리얼: 벽돌 적음, 기본 속도 느림
- **Stage 2** — 표준: 벽돌 많음, 기본 속도 중간
- **Stage 3** — 도전: 벽돌 많음, 기본 속도 빠름, 레이아웃 복잡

## 8. 저장 시스템

- **MVP**: 브라우저 localStorage (기기별 저장)
- **`SaveManager` 추상화 레이어 도입** — 내부만 교체해서 클라우드/IndexedDB로 확장 가능
- **저장 항목**:
  - 클리어한 최고 스테이지
  - 최고 점수

## 9. 클래스 구조 (초안)

```
Game                     // 게임 전체 컨트롤러
 ├─ GameState            // 현재 상태 (menu / playing / paused / cleared / gameover / ending)
 ├─ Stage                // 현재 스테이지 데이터 + 게임 오브젝트
 │    ├─ Ball
 │    ├─ Paddle
 │    └─ Brick[]
 ├─ InputHandler         // Pointer Events 처리
 ├─ Renderer             // Canvas 렌더링
 └─ SaveManager          // 저장/로드 추상화 (localStorage 백엔드)
```

구체 인터페이스는 Phase 2 시작 시 확정.

## 10. Out of Scope — v2 이후

### v2 (두 번째 웹 버전) 후보
- 파워업 (멀티볼, 패들 확장, 레이저)
- 특수 벽돌 (튼튼 / 폭발 / 아이템 / 불사)
- 사운드 / BGM
- 파티클 효과 / 화면 흔들림 / 슬로우모션
- 콤보 시스템 + 점수 배수
- 피버 모드
- 타임어택 모드 (시그니처 속도 메커니즘과 결합)
- 레벨 에디터 (웹 기반)
- 키보드 조작 지원

### Unity 포팅 단계
- TS 클래스를 C# MonoBehaviour로 이식
- 포스트프로세싱 (Bloom, Glow) 적용
- 클라우드 저장 (기기 간 동기화) — `SaveManager` 교체
- 플랫폼별 빌드 (WebGL, Windows, Android, iOS 등)
